<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-teal-50 to-teal-100 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-2xl w-full">
        <div class="bg-white rounded-2xl shadow-2xl p-8 md:p-12 text-center">
            <!-- Error Icon -->
            <div class="mb-6">
                <div class="inline-flex items-center justify-center w-24 h-24 bg-teal-100 rounded-full mb-4">
                    <i class="fas fa-exclamation-triangle text-teal-600 text-5xl"></i>
                </div>
            </div>

            <!-- Error Code -->
            <h1 class="text-8xl md:text-9xl font-bold text-teal-700 mb-4">404</h1>

            <!-- Error Message -->
            <h2 class="text-2xl md:text-3xl font-semibold text-gray-800 mb-4">Page Not Found</h2>
            <p class="text-gray-600 mb-8 text-lg">
                Oops! The page you're looking for doesn't exist. It might have been moved or deleted.
            </p>

            <!-- Action Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <button onclick="window.history.back()" class="inline-flex items-center justify-center px-6 py-3 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors font-medium">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Go Back
                </button>
                <a href="#" class="inline-flex items-center justify-center px-6 py-3 bg-teal-600 text-white rounded-lg hover:bg-teal-700 transition-colors font-medium shadow-lg">
                    <i class="fas fa-home mr-2"></i>
                    Back to Home
                </a>
            </div>

            <!-- Additional Links -->
            <div class="mt-12 pt-8 border-t border-gray-200">
                <p class="text-gray-600 mb-4">You might be looking for:</p>
                <div class="flex flex-wrap justify-center gap-4 text-sm">
                    <a href="#" class="text-teal-600 hover:text-teal-700 hover:underline">Dashboard</a>
                    <span class="text-gray-300">|</span>
                    <a href="#" class="text-teal-600 hover:text-teal-700 hover:underline">Products</a>
                    <span class="text-gray-300">|</span>
                    <a href="#" class="text-teal-600 hover:text-teal-700 hover:underline">Orders</a>
                    <span class="text-gray-300">|</span>
                    <a href="#" class="text-teal-600 hover:text-teal-700 hover:underline">Support</a>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="text-center mt-8 text-gray-600">
            <p class="text-sm">
                Need help? <a href="#" class="text-teal-600 hover:text-teal-700 font-medium">Contact Support</a>
            </p>
        </div>
    </div>

    <script>
        // Add a subtle animation on load
        window.addEventListener('load', function() {
            document.querySelector('.bg-white').style.animation = 'fadeIn 0.5s ease-in';
        });
    </script>

    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</body>
</html>